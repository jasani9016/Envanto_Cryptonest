import TermsCondition from "@/component/terms-condition";
import Wrapper from "@/layouts/Wrapper";

export const metadata = {
   title: "Term & Condition Page",
};
const index = () => {
   return (
      <Wrapper>
         <TermsCondition />
      </Wrapper>
   )
}

export default index